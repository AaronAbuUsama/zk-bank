{
  "name": "zk-bank",
  "module": "cli/index.ts",
  "type": "module",
  "bin": {
    "zk-bank": "./cli/index.ts"
  },
  "scripts": {
    "cli": "bun run cli/index.ts",
    "build": "bun cli init",
    "clean": "bun cli clean",
    "test": "bun cli test",
    "test:fork": "bun cli test --fork",
    "test:coverage": "bun cli test --coverage",
    "test:sync": "bun cli test-tree sync",
    "test:check": "bun cli test-tree check",
    "deploy": "bun cli deploy",
    "deploy:dry": "bun cli deploy --dry-run",
    "verify": "bun cli verify"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@aragon/osx": "1.4.0",
    "@aragon/osx-commons-contracts": "1.4.0",
    "@ensdomains/buffer": "^0.1.3",
    "@ensdomains/ens-contracts": "0.0.11",
    "@inkjs/ui": "^2.0.0",
    "commander": "^13.1.0",
    "glob": "^11.0.1",
    "ink": "^5.1.0",
    "react": "^18.3.1",
    "yaml": "^2.7.1"
  }
}
